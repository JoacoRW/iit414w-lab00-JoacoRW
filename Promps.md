## Context
I wanted IA to create a better writen and organize README to reproduce the things that i have done

## Prompts
Create a README.md that contains (a) Header: Your full name, GitHub username, course code (IIT414W), and date.
  (b) System info: Operating system (name + version), Python version, conda or pip version.
  (c) Setup instructions: Step-by-step commands to create the environment from your environment.yml or requirements.txt. Include the exact shell commands — do not assume the reader knows conda syntax.
  (d) How to run: Exact command(s) to execute each notebook. Specify if notebooks must be run in a particular order.
  (e) Problems encountered: Document at least two problems you hit during setup and how you solved them. These must be real — "no problems" is not credible and will be scrutinized.
  (f) Expected outputs: Brief description of what a successful run looks like (e.g., "setup_check.ipynb prints a 6-row package table and 'All checks passed ✓'").

## Relevant Output
The IA produce almost all the REAME.md that is used in this work

## Validation 
I read and run all the commands that the AI write on the README that he create and validate if it works or not.

## Adaptations  
The README that the AI gave me had extra things that werent usefull, so i deleted them and put the important parts to run my code.

## Final Decision 
Partial used, with a few modifications. Because It had many extra things that weren't necessary to run the enviroment that i creat, there was a faster way, that is the actual README.


## Context
I wanted IA to create a better .gitignore for me

## Prompts
i need u to create the .gitignore to IIT414W-LAB00-JOACORW

## Relevant Output
The IA produce the .gitignore

## Validation 
I read it and push the proyect to github to see if it works

## Final Decision 
i used the same code